name: quality

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled automation
        run: echo "Scheduled automation is intentionally not enabled in phase 0."
